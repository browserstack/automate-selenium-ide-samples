1. First install se-interpreter. Using npm install se-interpreter or npm install -g se-interpreter
2. Change username and access key in example_multi_config.json
3. To run it: se-interpreter example_multi_config.json
